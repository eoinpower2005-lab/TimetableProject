/**
 * this class represents a single scheduled timetable slot
 * a timetable slot links together the module, lecturer, room and studentGroup.
 * timetableSlot is used by the timetableManager which stores and manages the slots.
 */
public class timetableSlot {
}
